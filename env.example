DATABASE_URL=postgresql://junaid@localhost:5432/postgres
SESSION_SECRET=OH PLEASE PLEASE PLEASE PLEASE CHANGE ME

# Public Sign-ups Configuration
# Set to true to allow public sign-ups, false to restrict to admin-only user creation
PUBLIC_SIGNUPS_ALLOWED=false

# AI Chat Configuration
# These variables are used to power our chat that lets you 
# Handle dashboard functions using just text like adding products, regions, etc.
# It provides AI-powered interactions with your GraphQL API through MCP
# It uses the same auth as the logged in user and any functions the logged in User has access to, so be careful!

# SHARED AI KEYS (Optional)
# If you set these environment variables, ALL users will have access to use these shared API keys
# Users will see a "Shared Keys" option in the AI assistant configuration
# If you don't set these, users will only see the "Personal Keys" option and must provide their own API keys which are saved locally

# OpenRouter API Key - Get yours at https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# OpenRouter Model - Browse models at https://openrouter.ai/models
# Copy the model key (e.g., "openrouter/auto", "anthropic/claude-3.5-sonnet", etc.)
# Works best with openai/gpt-4o-mini and other models that support tool calling
# Be weary of using free models in production, you could be sending customer data to 3rd parties
OPENROUTER_MODEL=openrouter/auto